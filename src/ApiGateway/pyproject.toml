[project]
name = "simple-api-gateway"
version = "0.1.0"
description = "a microservice poc project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic_settings"
]

[dependency-groups]
dev = [
    "fastapi[standard]>=0.136.1",
    "ruff>=0.15.15",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = [
    "E", "F", "I", "FAST"
]

[tool.ruff.format]
quote-style = "single"